@model IEnumerable<projetfinsem.Models.Application>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nomapp)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date_creation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombre_telechargement)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Categorie.nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nomapp)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date_creation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombre_telechargement)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Categorie.nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.nom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idapp }) |
            @Html.ActionLink("Details", "Details", new { id=item.idapp }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idapp })
        </td>
    </tr>
}

</table>
